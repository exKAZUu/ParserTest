console.log(args != null);

for (var i = 0; i < args.length; i++) {
	console.log(1);
	for (var j = 0; args.length == 0; j++) {
		console.log("abc");
	}
}

while (args.length == 0) {
	while (args.length < 0) {
		i++;
	}
}

do {
	do {
		i = 0;
	} while (args.length < 0);
} while (args.length == 0);

if (args.length < 0) {
	if (args.length == 0) {
		func();
	}
}
